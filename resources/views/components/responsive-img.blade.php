<img 
    src="{{ asset($src) }}"
    alt="{{ $alt }}"
    width="{{ $width }}"
    height="{{ $height }}"
    loading="{{ $loading }}"
    fetchpriority="{{ $priority }}"
    decoding="async"
    class="{{ $class }}"
    style="width:100%;height:auto;object-fit:cover;display:block;"
    srcset="{{ $srcset }}"
    sizes="{{ $sizes }}"
/>